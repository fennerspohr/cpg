// Preload script - runs in renderer context before page loads
// Keep this minimal since we're loading a full Nuxt server
window.addEventListener('DOMContentLoaded', () => {
  // You can expose safe APIs here if needed in the future
})
